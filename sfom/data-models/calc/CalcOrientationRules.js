define(
	[

	],
	function(

	){
		return {

			/**
			 * @param {String | Object} format,
			 * @param {String} collageFormat
			 * return {String}
			 * */
			"get": function(format, collageFormat){

			},

			"rules": [
				{"format": "", "collageFormat": "", "orientation": "landscape"}
			]

		}
	}
);